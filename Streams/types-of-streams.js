// Streams are a way of sending and receiving data continuously, in small chunks instead of the whole piece of data at once. With the help of streams, we can transfer a large piece of data without requiring a lot of memory usage. That is because when we load some data, say a file, we are loading the data in memory. Now, if the file is a large file, then it will consume a lot of space in the memory. We can also take a concrete example of how this works.
// Say we have a file of 4 GiB. Now, without streams, when we load this file in our Node application, then 4 GiB of the memory will be used to store the file while we are performing some operations with it. If we want to send this file to the client, then, while the file is being sent, we will be storing it in memory. Now, if our VPS doesn't have sufficient memory, then, even loading the file onto the server is not possible.
// However, with the help of streams, we can make it possible. Now, instead of loading the whole 4 GiB of data at once, we only load a small chunk of it, say 100 MiB. So, we first load the first 100 MiB of the file and store it in our server while it is being sent to the client. After we get the acknowledgement from the client that the first 100 MiB of data has been transferred successfully, then, we can load the next 100 MiB of data onto the same location in memory by overwriting the previous data, because we are sure that the first 100 MiB has been successfully transferred. So, using this method, we can send a large file from the backend to the client without using a lot of memory.
// We can obviously note that this process, however, will be much slower than loading and sending the whole file at once. But, we are happyy to accept this limitation because without it, sending and receiving files wouldn't be always feasible. 

// Now, let's analyse the various types of streams we have. They are as follows:
// 1. Readable Stream -> As the name suggests, when we have access to some stream of data, we can only read from the stream and do something with it, but can't actually modify the contents of the stream. We can take an analogy of a flowing river for this. We can only consume the water flowing towards us from the river, but can't send the water backwards to the source.

// 2. Writable Stream -> Now, in a writable stream, we are only writing some data to the stream. The internal implementation will manage how the consumer will consume the data, but for us, the API facing us just has a write interface to which we can write data.

// 3. Duplex Stream -> In a duplex stream, both the processes involved have a read and a write interface. The read and write streams are completely independent, and thus we can also write to the writable part of the stream while reading from the readable part.

// 4. Transform Stream -> A transform stream is a special kind of duplex stream. We have both a read and a write interface, after the data is written to the write interface, the transform object transforms the data before it can be read from the read interface. And, that is the actual distinction of PERSPECTIVES. We are looking at the various types of stream objects from the perspective of the stream object itself.

// NOTE: The components connecting the 2 sides of the duplex stream needn't be 2 separate machines in a network sense. The components can be 2 functions inside the same program. We can take the example of a compression process. We create a duplex stream so that we have a readable and a writable interface from the stream, with a middle transformation portion.

// AHA! The thing that clicks these stream types is the PERSPECTIVE from where we are talking about the various types. And, THE PERSPECTIVE IS FROM THE STREAM OBJECT ITSELF. So, when we define the various types of streams, we are defining those from the perspective of the stream itself. Therefore, for a readable stream, the stream can only be read. We do not CARE how the data comes to the stream. We just know that this stream can only be read. Same is the case for a writable stream. Now, for a duplex stream, when we look from the perspective of the stream object itself, we have 2 separate interfaces so that the stream can be read, and the stream can also be used to write some data. Now, in the implementation, there will be 2 separate channels and therefore the parties connected via the stream can therefore read and write data independently, but, when we look from the perspective of the stream, we just know that it has both read and write interfaces. Finally, with this perspective, it does make sense that the transform stream is nothing special, because it will also have a read and a write interface. We do not care about the parties involved. But, this time, the object itself can perform some transformation on the stream data before it can be read from the read interface.